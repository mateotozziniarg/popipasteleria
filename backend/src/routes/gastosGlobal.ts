// Este archivo ya no se usa — reemplazado por routes/gastos.ts
// Se mantiene para evitar errores de import en index.ts hasta actualizar
import { Router } from 'express'
const router = Router()
export default router
